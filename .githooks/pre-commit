#!/usr/bin/env bash

set -e

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required for commits. Install it first (e.g. brew install gitleaks)." >&2
  exit 1
fi

gitleaks git --staged --redact .
